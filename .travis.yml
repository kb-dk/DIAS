language: ruby
notifications:
  email: false
rvm:
  - "1.9.3"
# uncomment this line if your project needs to run something other than `rake`:
script: bundle exec rake jetty:unzip jetty:config db:migrate db:seed db:test:prepare jetty:start spec cucumber jetty:stop