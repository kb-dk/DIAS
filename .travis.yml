language: ruby
notifications:
  email: false
rvm:
  - "1.9.3"
# uncomment this line if your project needs to run something other than `rake`:
script: bundle exec rake db:migrate db:seed jetty:config jetty:start db:test:prepare spec cucumber jetty:stop